import { useState } from "react";
import { Link } from "wouter";
import { trpc } from "@/lib/trpc";
import { SiteFooter, SiteHeader } from "./Home";
const tabs = ["Braids", "Twists", "Locs", "Kids Styles", "Add-ons"] as const;
export default function Services(){const [category,setCategory]=useState<typeof tabs[number]>("Braids"); const {data=[]}=trpc.public.services.useQuery({category}); return <div className="luxury-shell"><SiteHeader/><main className="container section-pad"><p className="pill w-fit">Services & pricing</p><h1 className="serif mt-4 text-6xl font-bold">Choose your next <span className="gold-text">luxury style.</span></h1><p className="mt-5 max-w-3xl text-white/65">Categorised pricing supports clear decisions before customers book. Each appointment requires a clearly labelled £20 non-refundable deposit at checkout.</p><div className="mt-10 flex flex-wrap gap-3">{tabs.map(t=><button key={t} onClick={()=>setCategory(t)} className={category===t?"btn-gold":"btn-dark"}>{t}</button>)}</div><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{(data as any[]).map(s=><article className="lux-card" key={s.id}><span className="pill text-xs">{s.badge || s.category}</span><h2 className="serif mt-4 text-3xl font-bold">{s.name}</h2><p className="mt-3 text-white/65">{s.description}</p><div className="mt-6 flex items-center justify-between border-t border-white/10 pt-5"><span className="text-sm text-white/60">{s.duration}</span><b className="text-2xl text-primary">From £{s.priceFrom}</b></div><Link href={`/booking?service=${encodeURIComponent(s.name)}`} className="btn-gold mt-5 w-full">Book This Style</Link></article>)}</div></main><SiteFooter/></div>}
